# Hopefully this is in the right place. Pytest docs describe *what* to put in
# pytest.ini file but are unhelpfully silent on *where* it's supposed to be located
[pytest]
markers =
    # Used to separate end-to-end tests from other tests
    e2e: mark test and an end-to-end test.